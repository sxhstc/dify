---
id: <禅道研发需求 Story ID 或其它系统需求 ID>
status: draft # 状态流转: draft -> reviewed -> approved
specType: added # added=新增规范文件 | modified=修改已有规范文件
targetSpec: "" # 仅当 specType=modified 时填写，指向被修改的原始规范文件路径，例如 specs/order/process.md
---

# Delta Spec: [规范标题]

> 本文件聚焦 **[影响领域/模块]** 的规范变更细节，是对 `../proposal.md §2.x [对应变更点标题]` 的精化。
> 与 proposal 的分工：proposal 描述**跨模块的全局变更意图**，本文件描述**本模块完整、可测试的行为规范**。

## 1. 需求描述 (EARS)

> 在 proposal 粗粒度 EARS 的基础上，**补充本模块的完整行为规范**，包括边界条件、异常流程和前置约束。
> 语法参考：*When \<触发条件\>, the \<系统/服务\> shall \<期望行为\>.*

**主流程**:
- *When [触发条件], the [系统/服务] shall [期望行为].*

**前置约束**:
- *If [前置条件不满足], the [系统/服务] shall [拒绝/降级行为].*

**异常与边界**:
- *If [异常条件], the [系统/服务] shall [容错行为].*
- *While [持续状态], the [系统/服务] shall [持续约束].*

## 2. 验收标准

> 比 proposal 验证计划更细粒度：每条标准须明确**前提条件、操作、预期结果**，可直接映射到测试用例。

- [ ] **正常场景**: 给定 [前提]，当 [操作] 时，系统 [预期结果]（可量化：响应时间/状态码/数据变化）
- [ ] **边界场景**: 给定 [边界值前提]，当 [操作] 时，系统 [边界预期结果]
- [ ] **异常场景**: 给定 [异常前提]，当 [操作] 时，系统 [错误处理结果，含提示文案/回滚行为]

## 3. 范围说明

**范围内 (In Scope)**:
- [本 Spec 负责覆盖的具体业务行为，与 proposal 中其他 Spec 不重叠]

**范围外 (Out of Scope)**:
- [与本模块相邻但属于其他 Spec 负责的行为，明确边界防止遗漏]
- 具体实现方案（由 `design.md` 承接）
