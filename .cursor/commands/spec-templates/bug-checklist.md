---
# BUG 提测清单元数据（生成时填写）
zentao-product-name: <禅道产品名>
缺陷id: <一个或多个禅道 Bug ID，与下表 BugId 一致>
提测类型: bug-fix
镜像id: <手动填写，可多个；与第二节逐行对应；未确认可用「待填写」>
生成时间: <ISO8601 或可读日期>
---

# BUG 提测清单 — <简短标题，如：Bug-5 等 N 项缺陷>

| | 中文名称 | ID |
| --- | --- | --- |
| 禅道产品 | <与 Frontmatter「zentao-product-name」一致> | `<productId>` |

---

## 一、缺陷列表

| BugId（禅道 Bug ID） | Bug 标题 | 状态 | 严重性 | 创建人 | 指派给 | 创建时间 |
| --- | --- | --- | --- | --- | --- | --- |
| `<id>` | `<title>` | `<status>` | `<severity 或 TBD>` | `<openedBy>` | `<assignedTo 或 —>` | `<openedDate 可读格式>` |

<!--
模板说明（写入产物时勿输出）：BugId 为禅道 `get_bugs_bugID` 返回的 `id`；多缺陷时多行。严重性优先取 severity 字段，无则 TBD。
-->

---

## 二、涉及改动的应用

| 应用名 | 镜像 id |
| --- | --- |
| `bfs-bemp` | <须确认后填写；未提供可「待填写」> |

---

## 三、SQL 脚本

<!--
模板说明（写入产物时勿输出）：无变更则本节留空，不要写「无」类套话。有变更时写 DDL/DML 或脚本路径。
-->

---

## 四、配置信息

| 应用 id | 命名空间 | 操作方式 | 配置项 | 对应值 | 是否运维修改 | 负责人 |
| --- | --- | --- | --- | --- | --- | --- |

---

## 五、测试关注点

- [ ] <回归缺陷复现路径已关闭>
- [ ] <相关接口/任务/消息兼容（按实际修复范围）>
- [ ] <边界与异常：空数据、超时、并发等>
- [ ] <数据：历史数据、幂等等>

---

## 六、相关文档（可选）

| 说明 | 仓库路径 |
| --- | --- |
| 本清单 | `onespec/checklist/bug-<slug>/bug-checklist.md` |
| 变更/归档（若存在） | `onespec/changes/<id>-*/` 或 `onespec/archive/*-<id>-*/` |

---

## 七、自测结果

| 场景 | 结果（通过/失败/跳过） | 说明 / 证据 |
| --- | --- | --- |
