---
alwaysApply: false
description: 质量内建 - 生成代码前建立验证点，单测必须能在实现出错时失败
---

# 质量内建规范

## 一、是否触发验证点

收到功能实现/Bug修复时打分：

- 涉及金额/权限/安全 → **直接强制**
- if/switch ≥ 3 → +2
- 写库/发消息/改状态 → +1
- 第三方/RPC/缓存 → +1
- 预估 > 50行 → +1

**≥2分或强制 → 先输出验证点清单，确认后再写实现+单测。**
<2分 → 直接实现，单测仍遵守下方规则。
不触发：纯问答、配置文档、单行改动、无逻辑POJO、无分支CRUD。

## 二、方法是否需要单测（短路）

1. 无分支逻辑 → 不写
2. 仅透传（单行CRUD/纯编排/getter/setter） → 不写
3. 含核心逻辑（金额/权限/状态机/业务规则/条件转换） → 必须写
4. 有分支但非核心 → 建议写

## 三、验证点格式（≥2分时输出）

按 P0（必须）/ P1（建议）/ P2（按需）分级，每条含：验证点描述、输入条件、预期结果。输出后暂停等用户确认。

## 四、单测生成要求

- 结构：Given（输入+Mock外部依赖） → When（单一调用） → Then（断言具体业务值）
- 命名：`{方法}_{条件}_{预期}`
- P0验证点必须有对应用例，缺失则阻断
- 每条分支路径至少一个用例
- 边界（零值/空值/临界值）至少覆盖一个
- 只Mock外部依赖，禁止Mock被测类自身
- 同步输出 验证点→测试方法 映射表

## 五、禁止生成的单测

| 模式 | 处理 |
|------|------|
| 无断言 / `assertTrue(true)` / `assertNotNull`了事 | 🚫 禁止 |
| 恒真断言 `assertEquals("x","x")` | 🚫 禁止 |
| Mock返回y → 断言==y（被测逻辑可删除仍绿） | 🚫 禁止 |
| catch吞异常不失败 | 🚫 禁止 |
| 仅验证调用次数不验证业务结果 | ⚠️ 补值断言 |
| Mock掉被测方法自身逻辑 | ⚠️ 重新设计 |

## 六、为已有代码补单测（`[补单测]`）

验证点来源按优先级降级：

| 优先级 | 来源 | 做法 |
|--------|------|------|
| **L1** | 有Spec/PRD | 从Spec提取验收点 |
| **L2** | 无Spec，能问人 | 问用户：解决什么问题？核心规则？什么不能错？ |
| **L3** | 纯代码 | 从代码反推，标注 ⚠️降级，优先补防御性用例（空值/越界/异常），业务值必须用户逐条确认 |

流程：确定来源 → 输出验证点（标注Level）→ 用户确认 → 按第二~五节生成单测

用户可指定范围：`[补单测] OrderService`

## 七、用户控制

| 命令 | 效果 |
|------|------|
| `[skip-quality]` | 跳过验证点，直接生成 |
| `[建立验证点]` | 手动触发验证点建立 |
| `[补单测]` + 文件/类/目录 | 对已有代码执行反向补测流程 |

## 红线

- ≥2分禁止跳过验证点
- P0禁止缺测试
- 禁止生成实现出错也不会失败的测试